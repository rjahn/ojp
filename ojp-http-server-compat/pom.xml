<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.openjproxy</groupId>
        <artifactId>ojp-parent</artifactId>
        <version>1.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>ojp-http-server-compat</artifactId>
    <packaging>war</packaging>
    <name>OJP HTTP Servlet Server (compat)</name>
    <description>HTTP/HTTPS servlet transport for OJP server (compatibility)</description>

    <dependencies>
        <dependency>
            <groupId>org.openjproxy</groupId>
            <artifactId>ojp-http-server</artifactId>
            <version>${project.version}</version>
            <type>war</type>
        </dependency>
    </dependencies>

    <build>
        <plugins>
	    <plugin>
        	<groupId>org.apache.maven.plugins</groupId>
        	<artifactId>maven-compiler-plugin</artifactId>
        	<version>3.4.0</version>
        	<executions>
            	    <execution>
                	<id>default-compile</id>
                	<phase>none</phase>
            	    </execution>
            	    <execution>
                	<id>default-testCompile</id>
                	<phase>none</phase>
            	    </execution>
        	</executions>
    	    </plugin>

            <plugin>
	        <groupId>org.apache.maven.plugins</groupId>
    	        <artifactId>maven-war-plugin</artifactId>
    		<version>3.4.0</version>
        	<configuration>
            	    <failOnMissingWebXml>false</failOnMissingWebXml>
        	</configuration>
    	    </plugin>        
        
            <plugin>
                <groupId>org.eclipse.transformer</groupId>
                <artifactId>transformer-maven-plugin</artifactId>
                <version>1.0.0</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <id>transform-war</id>
                        <phase>package</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
			    <artifact>
                		<groupId>org.openjproxy</groupId>
	                        <artifactId>ojp-http-server</artifactId>
        		        <version>${project.version}</version>
                		<type>war</type>
	                    </artifact>
			    <rules>
		                <stripSignatures>true</stripSignatures>
                		<jakartaDefaults>true</jakartaDefaults>
                		<invert>true</invert>
            		    </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>                
        </plugins>
    </build>
</project>
